import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import morgan from 'morgan'; import rateLimit from 'express-rate-limit'; import { config } from './config.js';
import auth from './routes/auth.js'; import users from './routes/users.js'; import listings from './routes/listings.js'; import social from './routes/social.js'; import messages from './routes/messages.js'; import account from './routes/account.js'; import admin from './routes/admin.js'; import events from './routes/events.js'; import living from './routes/living.js'; import connections from './routes/connections.js'; import { notFound,errorHandler } from './middleware.js';
export const app=express(); app.use(helmet()); app.use(cors({origin:config.clientUrl})); app.use(express.json({limit:'1mb'})); app.use(morgan('dev')); app.use('/api/auth',rateLimit({windowMs:15*60*1000,limit:100}),auth); app.use('/api/users',users); app.use('/api/listings',listings); app.use('/api/social',social); app.use('/api/messages',messages); app.use('/api/events',events); app.use('/api/living',living); app.use('/api/connections',connections); app.use('/api',account); app.use('/api/admin',admin); app.get('/api/health',(req,res)=>res.json({status:'ok'})); app.use(notFound); app.use(errorHandler);
